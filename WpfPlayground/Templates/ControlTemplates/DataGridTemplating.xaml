<Window x:Class="WpfPlayground.Templates.ControlTemplates.DataGridTemplating"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPlayground.Templates.ControlTemplates"
        mc:Ignorable="d"
        Title="DataGridTemplating" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="1" ItemsSource="{Binding EmpList}" />
    </Grid>
</Window>
